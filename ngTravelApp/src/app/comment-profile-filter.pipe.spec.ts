import { CommentProfileFilterPipe } from './comment-profile-filter.pipe';

describe('CommentProfileFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentProfileFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
